<div id="textslider">
    <div id="carousel-example-generic" class="carousel slide" data-ride="carousel">
  
        <!-- Indicators -->
        <ol class="left carousel-indicators">
        {% for i in nodesArray %}
            <li data-target="#carousel-example-generic" data-slide-to="{{ loop.index - 1}}" class="active"></li>
        {% endfor %}
        
        

        </ol>
          
        <!-- Wrapper for slides -->
        <div class="carousel-inner" >
            {% for node in nodesArray %}
            {% if loop.index == 1 %}
                <div class="item active" style="padding: 5px;">
                    <div style="display: table; margin: 0 auto;">
                    <h3><a href="node/{{ node.id}}">{{ node.title}}</a></h3>
                    </div>
                    <div style="display: table; margin: 0 auto;width: 687px;text-align: justify;text-justify: inter-word;">{% filter striptags %}{{ node.body|raw }}{% endfilter %} read more .....</div>
                </div>
            {% else %}
                <div class="item" style="padding: 5px;">
                    <div style="display: table; margin: 0 auto;">
                    <h3><a href="node/{{ node.id}}">{{ node.title}}</a></h3>
                    </div>
                    <div style="display: table; margin: 0 auto;width: 687px;text-align: justify;text-justify: inter-word;">{% filter striptags %}{{ node.body|raw }}{% endfilter %} read more .....</div>
                </div>
            {% endif %}
            {% endfor %}
        </div>
        
        <!-- Controls 
        <a class="left carousel-control" href="#carousel-example-generic" data-slide="prev">
            <span class="glyphicon glyphicon-chevron-left"></span>
        </a>
        
        <a class="right carousel-control" href="#carousel-example-generic" data-slide="next">
            <span class="glyphicon glyphicon-chevron-right"></span>
        </a>
        -->
    </div>
</div>














